pipeline {
    agent { label 'slavenode' }

    stages {
        stage('build'){
            steps{
                dir('To-Do-App'){
                    sh 'yarn install'
                    sh 'yarn test'
                    echo 'hola'
                }
            }
        }
        stage('Test'){
            steps{
                dir('To-Do-App'){
                    sh 'docker exec app-container yarn test'
                }
            }
        }
    }
}
